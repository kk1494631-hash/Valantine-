<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sara 💖 My Valentine</title>

<style>
  * { box-sizing: border-box; }

  body {
    margin: 0;
    min-height: 100vh;
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 15px;
    overflow: hidden;
  }

  .card {
    background: white;
    width: 100%;
    max-width: 380px;
    padding: 30px 22px;
    border-radius: 26px;
    box-shadow: 0 18px 40px rgba(0,0,0,0.25);
    text-align: center;
    position: relative;
    z-index: 2;
  }

  h1 {
    font-size: 34px;
    margin: 0;
    color: #ff4d6d;
  }

  h2 {
    font-size: 26px;
    margin: 12px 0;
    color: #333;
  }

  p {
    color: #555;
    font-size: 18px;
  }

  .buttons {
    margin-top: 32px;
    position: relative;
    height: 150px;
  }

  button {
    padding: 15px 28px;
    border: none;
    border-radius: 32px;
    font-size: 18px;
    cursor: pointer;
  }

  #yesBtn {
    background: #ff4d6d;
    color: white;
  }

  #noBtn {
    background: #ddd;
    color: #333;
    position: absolute;
    left: 0;
    top: 0;
  }

  .letter {
    display: none;
    font-size: 19px;
    line-height: 1.7;
  }

  /* Floating hearts */
  .heart {
    position: absolute;
    bottom: -20px;
    font-size: 22px;
    animation: floatUp 6s linear infinite;
    opacity: 0.85;
  }

  @keyframes floatUp {
    0% { transform: translateY(0); opacity: 0; }
    10% { opacity: 1; }
    100% { transform: translateY(-110vh); opacity: 0; }
  }
</style>
</head>

<body>

<div id="hearts"></div>

<audio id="bgMusic" loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<div class="card" id="mainCard">
  <h1>💖</h1>
  <h2>Hey Sara</h2>
  <p>
    This is something simple,<br>
    honest, and just for you.
  </p>
  <p style="font-size:20px; margin-top:18px;">
    Will you be my Valentine?
  </p>

  <div class="buttons" id="buttonBox">
    <button id="yesBtn">Yes 💕</button>
    <button id="noBtn">No 🙃</button>
  </div>
</div>

<div class="card letter" id="letterCard">
  <h2>💌 Dear Sara</h2>
  <p>
    I know we’re not perfect,<br>
    and sometimes emotions get heavy.<br><br>

    But when things are calm,<br>
    when we laugh, talk, eat momos or pizza,<br>
    those moments feel real to me.<br><br>

    I don’t want to control you,<br>
    or pressure you,<br>
    or prove anything.<br><br>

    I just want to choose you,<br>
    honestly and gently.<br><br>

    Today, that’s all this is.<br><br>

    Will you be my Valentine? ❤️
  </p>
</div>

<script>
  const noBtn = document.getElementById("noBtn");
  const yesBtn = document.getElementById("yesBtn");
  const mainCard = document.getElementById("mainCard");
  const letterCard = document.getElementById("letterCard");
  const box = document.getElementById("buttonBox");
  const music = document.getElementById("bgMusic");
  const heartsContainer = document.getElementById("hearts");

  function moveNoButton() {
    const maxX = box.clientWidth - noBtn.offsetWidth;
    const maxY = box.clientHeight - noBtn.offsetHeight;
    noBtn.style.left = Math.random() * maxX + "px";
    noBtn.style.top = Math.random() * maxY + "px";
  }

  noBtn.addEventListener("touchstart", moveNoButton);
  noBtn.addEventListener("mouseover", moveNoButton);

  yesBtn.addEventListener("click", () => {
    mainCard.style.display = "none";
    letterCard.style.display = "block";
    music.play();
    startHearts();
    confettiBlast();
  });

  function startHearts() {
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = ["💖","💕","💘","💞"][Math.floor(Math.random()*4)];
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (4 + Math.random() * 3) + "s";
      heartsContainer.appendChild(heart);
      setTimeout(() => heart.remove(), 7000);
    }, 280);
  }

  function confettiBlast() {
    for (let i = 0; i < 120; i++) {
      const confetti = document.createElement("div");
      confetti.style.position = "fixed";
      confetti.style.width = "8px";
      confetti.style.height = "14px";
      confetti.style.background =
        ["#ff4d6d","#ff9a9e","#fad0c4","#ffd166","#ff85a1"]
        [Math.floor(Math.random()*5)];
      confetti.style.left = Math.random() * 100 + "vw";
      confetti.style.top = "-10px";
      confetti.style.opacity = 0.9;
      confetti.style.transform = `rotate(${Math.random()*360}deg)`;
      confetti.style.transition = "top 2.5s ease-out, transform 2.5s";
      document.body.appendChild(confetti);

      setTimeout(() => {
        confetti.style.top = "110vh";
        confetti.style.transform += " rotate(720deg)";
      }, 10);

      setTimeout(() => confetti.remove(), 2600);
    }
  }
</script>

</body>
</html>